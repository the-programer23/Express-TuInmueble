<!DOCTYPE html>

<html>

    <head>
        <title>Propietario</title>
         <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <link href="https://fonts.googleapis.com/css?family=Lato:100,300,300i,400&display=swap" rel="stylesheet" type="text/css">
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    </head>

    <body>
        {{>header}}
            <div class="main-content">
                <h1>Propietario</h1>
                <div class="propietario-navbar">
                    <ion-icon name="home"></ion-icon>
                    <a href="/propietario/inmuebles">Tus inmuebles</a>
                    <a href="/propietario/escogerInmobiliaria">Escoger inmobiliaria</a>
                    <a href="/propietario/agregarInmueble">Agregar inmueble</a>
                </div> 
                <h2>Tus inmuebles</h1>
                <div class="ownerPropertiesContainer">
                    <span class="title">{{realStateType}}</span>
                    <label>Barrio</label>
                    <span>{{ownerProperties.neighborhoodName}}</span>
                    <label>Dirección</label>
                    <span>{{ownerProperties.address}}</span>
                    <label>Ciudad</label>
                    <span>{{ownerProperties.city}}</span>
                    <label>País</label>
                    <span>{{ownerProperties.country}}</span>
                    <label>Valor de la renta</label>
                    <span>{{ownerProperties.rentCost}}</span> <br>
                    <button>Vincular a una Inmobiliaria</button>
                </div>
            </div>
         {{>footer}}
         <script src="https://unpkg.com/ionicons@4.5.10-0/dist/ionicons.js"></script>
    </body>
    
</html>